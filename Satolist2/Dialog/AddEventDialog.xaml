<Window x:Class="Satolist2.Dialog.AddEventDialog"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:Satolist2.Dialog"
		mc:Ignorable="d"
		Title="新しい項目を追加する" Height="450" Width="800">
	<Grid>
		<StackPanel>
			
			<TextBlock Text="項目の名前" />
			<TextBox Text="{Binding Name}" />
			
			<TextBlock Text="追加先"/>
			<ComboBox SelectedItem="{Binding AddTarget}" ItemsSource="{Binding Main.Ghost.Dictionaries}" DisplayMemberPath="RelativeName" />
			<StackPanel Orientation="Horizontal">
				<RadioButton Content="文" IsChecked="{Binding IsSentenceSelected}" />
				<RadioButton Content="単語群" IsChecked="{Binding IsWordSelected}" />
			</StackPanel>
			
			<StackPanel Orientation="Horizontal">
				<Button Content="追加" IsDefault="True" Click="OkButtonClick" />
				<Button Content="キャンセル" IsCancel="True" Click="CancelButtonClick" />
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
