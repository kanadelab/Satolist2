<UserControl x:Class="Satolist2.Control.SurfaceViewer"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:Satolist2.Control"
			 xmlns:util="clr-namespace:Satolist2.Utility"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<util:BitmapImageSourceConverter x:Key="BitmapConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!--
		<Menu Grid.Row="0">
			<MenuItem Header="ファイル" />
		</Menu>
		-->
		<TextBox Text="{Binding ShellPath, Mode=TwoWay}" />
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="auto"/>
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<ListView Grid.Column="0" ItemsSource="{Binding SurfaceIDList}" SelectedItem="{Binding SelectedSurfaceID}" />
			<Image Grid.Column="1" Source="{Binding SelectedSurfaceBitmap, Converter={StaticResource BitmapConverter}}" Width="{Binding SelectedSurfaceBitmap.Width}" />
		</Grid>
	</Grid>
</UserControl>
